from setuptools import find_packages, setup

setup(
    name="simple-sphinx",
    packages=find_packages(include=["simple_sphinx"]),
    version='0.1.0',
    description='Example sphinx project with markdown',
)
